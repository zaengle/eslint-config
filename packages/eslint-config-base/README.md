# Base ESLint Config

This is our base ESLint config for projects that do **not** use a JavaScript framework (e.g., Vue, React).

## Usage

```js
// In .eslintrc.js

{
    extends: '@zaengle/eslint-config-base'
}
```
