{
  "name": "eslint-config",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present"
  },
  "devDependencies": {
    "eslint": "^10.0.0",
    "lerna": "^9.0.7"
  },
  "overrides": {
    "axios": "^1.19.0",
    "follow-redirects": "^1.16.0",
    "yaml": "^2.8.4",
    "nx": {
      "brace-expansion": "^5.0.5"
    }
  }
}
